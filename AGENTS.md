# Agent 時代的知識工作

這個 repo 是「Agent 時代的知識工作」課程的 GitHub 入口，同時也是課程自己的示範專案：
它用課程教的方法維護——這份 AGENTS.md、`kb/` 知識庫、`工作紀錄.md`。

## 四個階段與資料夾

想法從發散到發布走四段，每段對應固定位置。判斷一個東西該放哪裡，看它處在哪一段，不看它有多長。

| 階段 | 位置 | 放什麼 | 誰動手 | 發布 |
|---|---|---|---|---|
| 發散 | 對話，以及 `工作紀錄.md` 的「待消化」區 | 亂聊出來、還沒決定收不收的想法。落檔時標明誰提出、日期、候選用法 | agent 寫，人決定 | 否 |
| 整理 | `kb/` | 找到的資料的摘要：一個來源或一個主題一檔，固定四段：來源、重點、洞見、課程用途（候選）。用 `[[檔名]]` 互連 | agent 寫 | 否，但在 git 裡 |
| 收斂成初稿 | `drafts/` | 依主題從 kb 與待消化區抽出來排成結構的東西：課程地圖、教案、矩陣、大綱。可以大改 | agent 起草，人挑選 | 否，`_quarto.yml` 排除 |
| 最終交付 | `lessons/`、`index.qmd`、`setup.qmd`、`tools.qmd`、`slides/` | 學員看得到的成品：文章版教材、首頁、環境建立、工具索引、投影片 | agent 寫，人審過才發布 | 是：網站走 GitHub Pages，投影片走 OneDrive 共用連結 |

橫向支撐三段的：

- `工作紀錄.md`：除了待消化區，還有「已消化並整合」與各次定案，是決策的紀錄。
- `AGENTS.md`、`skills/`：定案後的工作方式與判斷標準，每次對話都讀。`skills/` 一個主題一檔。
- `assets/`：教材圖片，必須附來源與授權；`assets/site/` 放網站圖示。

### 升格規則

- 待消化 → kb：資料讀過、有來源、能寫出洞見，就成一檔。
- kb → drafts：使用者指定主題後才抽；drafts 裡每份要註明取自哪些 kb 檔與待消化項。
- drafts → lessons 與 slides：只在使用者說「整合」或「發布」之後。這一步等於對外交付，agent 不自行升格。
- 反向也成立：教材發現問題，退回 drafts 改結構，不在 lessons 上硬修。

## 工作模式

### 先發散，不急著整合

使用者常常不知道要寫什麼，會先問 agent 有沒有想法。這時：

1. 直接給想法，不要反問「你想要什麼」。空白頁最貴，先有東西可以反應。
2. 使用者對想法的反應（不對、太像、方向錯）比憑空想「對的」快得多，這是正常流程。
3. **agent 提出的東西不直接寫進正式文件。** 先進 `工作紀錄.md` 的「待消化」區或 `kb/`，照上面的升格規則往前走。
4. 使用者決定採用後，把待消化項移到「已消化並整合」，附一句理由。

### 依任務選模型與分工

使用者授權主責 agent 在本專案內，主動將適合獨立處理的子任務交給其他 agent，並依當時可用的模型選擇能力等級與推理強度，不需每次另問。主責是協調與交付的角色，不代表一定使用最強模型；也可以向能力更強的模型求助。

| 任務特性 | 選用原則與例子 |
|---|---|
| 範圍明確、重複、容易驗收 | 優先輕量、快速、成本較低的模型，例如檔案盤點、擷取指定欄位、整理已確認的來源、格式轉換、連結檢查 |
| 需要有限判斷與局部整合 | 使用通用、均衡的模型，例如依既有判準整理段落、比較少量材料、完成局部修改 |
| 模糊、多步推理、證據衝突或錯誤代價高 | 直接選較強的推理模型，例如判斷研究能否支持論點、跨來源推論、課程結構取捨、複雜除錯；不必先讓輕量模型試錯 |

- **動態選擇，不固定型號。** 依當前工具提供的可用模型清單、能力說明、工具與輸入支援，兼顧速度、成本及本次驗收要求；需要比較而資訊不足時再查官方資料，不憑名稱、新舊或價格猜能力。模型等級與推理強度分開決定，只選工具實際支援的組合。不能指定模型時照可用能力工作，必要時說明限制，不聲稱已切換。
- **分工要值得。** 先考慮直接工具或腳本能否完成。子任務能獨立進行、主責同時有其他工作可做，或需要專門判斷時才委派；很短的小改動直接完成。計入交代背景、等待、查核與返工的成本，不為節省單次呼叫而增加整體工作量。
- **交辦可驗收的工作。** 提供目標、必要背景、檔案或來源位置、可修改範圍與完成條件，只傳需要的脈絡。要求回報成果、依據、已做檢查與未解問題；避免多個 agent 同時修改同一區段。主責負責協調與整合，不重做已交辦的整項工作。
- **遇到難題可以向上求助。** 接手的模型發現關鍵推論無把握、證據互相矛盾、任務超出能力，或一次針對性修正後仍過不了驗收，就回報卡點、已試方法與證據，由主責安排較強模型協助；有明確必要且工具允許時，也可直接委派較強模型處理該子問題。避免反覆猜測、層層轉包或循環求助。
- **先辨認卡點。** 缺資料、缺權限或需求未定時，補資料或交回主責釐清；換強模型不能解決這些限制。難題解開後，剩餘例行工作可交回原模型繼續，不必整項升級。
- **主責仍要驗收。** 按錯誤代價與可驗證程度檢查成果；關鍵論點核對原始證據，格式與檔案操作用適當工具檢查。不能因模型較強就直接採信，也不必對所有雜事再做一次完整人工式審查。簡短交代有意義的分工與升級理由即可。

### 收納素材

使用者丟連結、引文、觀察時：

1. 存成 `kb/` 一則筆記：來源、重點、洞見、課程用途（候選，未定）。
2. 跟既有 kb 筆記有關就用 `[[檔名]]` 連起來。
3. 不要為每個小想法開新檔；相近主題補進既有筆記。

### 脈絡保存

每個階段的結論落成檔案並 commit，讓討論脈絡可以帶到下一次對話。commit 訊息寫「為什麼」。
使用者與 agent 的互動模式本身也是課程素材——觀察到可教學的工作流時先提出，確認後記進工作紀錄。

### 寫正式教材

- `lessons/*.qmd` 只輸出文章版：把投影片與口頭講解補寫成可獨立閱讀的內容。
- 正文以 `##` 表示主要章節、`###` 表示小節。強調句用 `####`，避免進入右側大綱。
- 教材寫成工具無關：只給任務與素材，不綁特定 agent 的指令。
- 文案語氣照 `skills/chinese-copy-style.md`：平實、簡短，不用金句口號，動詞用工整的書面詞。使用者修正語句時，把修改前後補進該檔的「修改對照」。
- 投影片以 agent 產 `.pptx` 為主，中間產物（論點、故事線）留在 `drafts/`。

## 課程核心

- 課程不是教 agent，也不是教做簡報：每週先講一件知識工作本來怎麼做（要素、判準、常見失敗），再拆解流程，分辨哪些環節交給 agent、哪些必須人把關。簡報是第一個練習對象。
- Agent 是放大器，放大的是人本來就會的東西；判斷力來自懂那件事，不會做的人審不出 agent 的產出哪裡有問題。判斷是最貴、不能外包的環節。
- 每週固定節奏：本行怎麼做 → 拆解流程 → agent 做、依判準檢視 → 找出通順卻沒有判斷的段落（slop）→ 把判準寫入自己的 AGENTS.md 或 skill。學員的 repo 就是自己判準的集合。
- 骨架仍是收集 → 整理 → 蒸餾 → 表達；發散用 vibe，收斂用結構。
- 選模型講成配置題（模型等級、推理強度、額度），不推銷；建議學員訂閱最低方案，教材仍寫成工具無關。
- 已定進度只到第四週；第五週之後依署內需求滾動排課，不預先展開。

## 維護原則

- 保持文字化：教案、筆記、教材都用 Markdown 或 Quarto Markdown。
- 保持安全：不提交密碼、API key、署內真實資料、PPTX、Word；圖片放 `assets/` 並附來源。
- 保持可迭代：小步更新、每次對應明確目的。
- 與第一門課（AI Agent 開發與維運）方法同源，但敘事獨立；提到共通處一句帶過即可，不要做平行對照表。
