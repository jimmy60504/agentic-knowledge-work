# 第四週 4-2 與 4-3 Story 圖

本資料夾保存第四週 4-2、4-3 網頁文章使用的 Story 圖 PNG。來源為 `slides/story/` 下同名的 PlantUML 檔案，並由 `slides/story/render.py` 產生。

這些圖與 `slides/week4-2.pptx`、`slides/week4-3.pptx` 使用同一批來源。修改圖面時應先修改 `.puml`，重新產圖後再同步本資料夾，不應直接修改 PNG。
